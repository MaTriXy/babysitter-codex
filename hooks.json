{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT:-}}\" ]; then exec bash \"${CLAUDE_PLUGIN_ROOT:-$PLUGIN_ROOT}/hooks/babysitter-proxied-session-start.sh\"; fi"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT:-}}\" ]; then exec bash \"${CLAUDE_PLUGIN_ROOT:-$PLUGIN_ROOT}/hooks/babysitter-proxied-stop.sh\"; fi"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT:-}}\" ]; then exec bash \"${CLAUDE_PLUGIN_ROOT:-$PLUGIN_ROOT}/hooks/babysitter-proxied-user-prompt-submit.sh\"; fi"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT:-}}\" ]; then exec bash \"${CLAUDE_PLUGIN_ROOT:-$PLUGIN_ROOT}/hooks/babysitter-proxied-pre-tool-use.sh\"; fi"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT:-}}\" ]; then exec bash \"${CLAUDE_PLUGIN_ROOT:-$PLUGIN_ROOT}/hooks/babysitter-proxied-post-tool-use.sh\"; fi"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT:-}}\" ]; then exec bash \"${CLAUDE_PLUGIN_ROOT:-$PLUGIN_ROOT}/hooks/babysitter-proxied-session-end.sh\"; fi"
          }
        ]
      }
    ]
  }
}
